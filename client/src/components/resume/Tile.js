import React from "react"

function Tile({file}) {
    // const fname = {file.name};
    // const thumbnail = {file.preview_image};

    return (
        <p>This component should return a thumbnail of the file and allow it to be viewed on the web or downloaded.</p>

    )
    
}

export default Tile;